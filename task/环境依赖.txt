操作系统： Windows11、macOS或主流64位Linux。

主软件为Node.js，使用支持ESM、node:fs、node:path和node:url的兼容版本。项目入口由package.json提供，不依赖第三方npm包。

执行账号需要读取input_data目录并可写output目录。消息分片、成员清单、投递回执和遮盖规则均在本机处理，不连接聊天系统或外部接口。

当前材料适合普通64位多核CPU和常规办公内存，不使用GPU。磁盘需容纳输入分片、处理缓冲和四份报告。运行入口应设置有限超时，避免异常输入长期占用进程。
